#Must be copied to /etc

#!/usr/sbin/nft -f

flush ruleset

table inet filter {
    chain input {
        type filter hook input priority 0; policy accept;
        # Accept all for now. Tighten in production.
    }

    chain forward {
        type filter hook forward priority 0; policy accept;
        # Accept all forwarding for now.
        # When Ryu + OVS OpenFlow rules are in place, we can
        # drop this back to policy drop and let OVS handle decisions.
    }

    chain output {
        type filter hook output priority 0; policy accept;
    }
}

table ip nat {
    chain prerouting {
        type nat hook prerouting priority dstnat; policy accept;
        # Redirect any DNS query from the IoT subnet directly to AdGuard's
        # container IP, bypassing Docker's port mapping entirely.
        iifname "br0" ip saddr 192.168.50.0/24 ip daddr != 192.168.50.1 udp dport 53 dnat to 172.20.0.53
        iifname "br0" ip saddr 192.168.50.0/24 ip daddr != 192.168.50.1 tcp dport 53 dnat to 172.20.0.53
    }
    chain postrouting {
        type nat hook postrouting priority srcnat; policy accept;
        # Masquerade IoT subnet traffic going out via enp2s0
        oifname "enp2s0" ip saddr 192.168.50.0/24 masquerade
    }
}
