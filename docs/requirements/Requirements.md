 _Project Requirements_

<div class="joplin-table-wrapper"><table><thead><tr><th><p><strong>ID</strong></p></th><th><p><strong>Requirement</strong></p></th><th><p><strong>Priority</strong></p></th><th><p><strong>Acceptance Criteria</strong></p></th><th><p><strong>Rationale</strong></p></th></tr></thead><tbody><tr><td colspan="5"><p><strong>Functional Requirements</strong></p></td></tr><tr><td><p>FR-01</p></td><td><p>Visible Wi-Fi<br>Network</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p></td><td><ol><li>The SSID IoT-Security-AP is visible to a scanning device, confirmed using&nbsp;“nmcli&nbsp;dev wifi list”&nbsp;from the device.&nbsp;</li><li>A device can successfully associate with the network using WPA2-PSK credentials, confirmed by completing the connection handshake without error.&nbsp;&nbsp;</li></ol></td><td><p>Without a functioning access point, no IoT device can connect and the system cannot function.&nbsp;</p></td></tr><tr><td><p>FR-02</p></td><td><p>Essential Services</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p><p></p></td><td><ol><li>A device joining the network receives an IP address, subnet mask, default gateway, and DNS server via DHCP within 10 seconds of connecting, confirmed by inspecting the device's network configuration.&nbsp;</li><li>A DNS query issued from a connected device returns a valid response, confirmed using&nbsp;‘nslookup’&nbsp;or&nbsp;‘dig’&nbsp;from the device.&nbsp;</li><li>NTP synchronisation succeeds for a connected device, confirmed by verifying the device clock is within 1 second of the correct time after joining.&nbsp;</li><li>ARP resolution succeeds between a connected device and the gateway, confirmed by verifying the gateway MAC address appears in the device's ARP table.&nbsp;</li></ol></td><td><p>These are essential services that are pre-requisites for any network activity.&nbsp;</p></td></tr><tr><td><p>FR-03</p></td><td><p>Network Isolation</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p><p></p></td><td><ol><li>A packet that does not match a rule explicitly permitting it is dropped and does not reach its destination, confirmed by&nbsp;attempting&nbsp;to send traffic on an unpermitted port and&nbsp;observing&nbsp;no response.&nbsp;</li><li>A port scan of the IoT subnet from an external host should return no reachable addresses, confirmed by running a port scan from a device connected to</li></ol></td><td><p>The default deny policy is the security background of the system that creates the network isolation; without it, devices are not meaningfully protected.&nbsp;</p></td></tr><tr><td><p>FR-04</p></td><td><p>Prevention of Lateral</p><p>Movement</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p><p></p></td><td><ol><li>Device A cannot ping Device B on the IoT subnet, confirmed by a running&nbsp;an&nbsp;ICMP ping between two connected test devices&nbsp;and&nbsp;observing&nbsp;100% packet loss.&nbsp;</li><li>Device A cannot reach any open port on Device B, confirmed by running a port scan from Device A targeting Device B's IP and receiving no responses.&nbsp;</li></ol></td><td><p>Lateral movement&nbsp;is one of the key attack vectors for IoT networks.&nbsp;</p></td></tr><tr><td><p>FR-05</p></td><td><p>DNS Filtering</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p><p></p></td><td><ol><li>A DNS query sent from a device with a hardcoded resolver of 8.8.8.8 is intercepted and answered by the gateways DNS resolver, confirmed by querying a domain on the gateway resolvers blocklist and&nbsp;observing&nbsp;it is blocked despite the hardcoded resolver.&nbsp;</li><li>A query for a domain on the gateways&nbsp;blocklist&nbsp;returns no valid IP address, confirmed by querying the domain from a connected device and&nbsp;observing&nbsp;an NXDOMAIN or blocked response.&nbsp;</li><li>A query for a&nbsp;legitimate domain resolve&nbsp;successfully, confirmed by querying a known-good domain and receiving a valid IP address.&nbsp;</li></ol></td><td><p>Without forced DNS&nbsp;interception devices can bypass filtering using a&nbsp;hardcoded DNS resolver.&nbsp;&nbsp;</p></td></tr><tr><td><p>FR-06</p></td><td><p>Automated Device Isolation</p></td><td><p><strong>Must</strong></p><p><strong>Have</strong></p></td><td><ol><li>When an anomaly meeting the isolation threshold is detected, the offending device loses all network connectivity within 5 seconds, confirmed by monitoring ICMP ping from the device&nbsp;when&nbsp;an isolation-triggering event is simulated.&nbsp;</li><li>The isolated device's traffic does not appear in&nbsp;subsequent&nbsp;connection logs, confirmed by inspecting connection logs&nbsp;after isolation is applied.&nbsp;</li></ol></td><td><p>Manual and automated isolation are only possible if there is an API to&nbsp;facilitate&nbsp;interactions. The API also&nbsp;facilitates&nbsp;both manual and automated created of per-device&nbsp;destination&nbsp;allowlists&nbsp;&nbsp;</p></td></tr><tr><td><p>FR-07</p></td><td><p>Passive Traffic Analysis</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>Presence of, at minimum, connection logs, http logs, DNS logs, and SSL/TSL logs within 60 seconds of a connected device generating the relevant traffic type, confirmed by inspecting the log directory after generating test traffic.&nbsp;</li></ol></td><td><p>Logging is essential for detection and interception, but core security&nbsp;policies&nbsp;can be enforced without it.&nbsp;&nbsp;</p></td></tr><tr><td><p>FR-08</p></td><td><p>Per-Device Destination Allowlists</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>A device with an enforced allowlist cannot&nbsp;initiate&nbsp;a connection to a destination outside it allowlist, confirmed by&nbsp;attempting&nbsp;a connection to an IP not in the profile and&nbsp;observing&nbsp;it is blocked.&nbsp;</li><li>A device with an enforced allowlist can successfully reach all destinations defined in the&nbsp;allowlist, confirmed by connecting to each permitted destination and receiving a valid response.&nbsp;</li><li>Allowlist changes applied via the REST API take effect without restarting any components, confirmed by adding a new destination and verifying connectivity to it&nbsp;immediately&nbsp;after.&nbsp;</li></ol></td><td><p>Significantly strengthens the security posture of the system but the system will be functional&nbsp;and provide protection&nbsp;without it.&nbsp;</p></td></tr><tr><td><p>FR-09</p></td><td><p>Anomaly Detection</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>All detection logic produces a structured alert entry in the&nbsp;logs when the relevant anomaly condition is triggered, confirmed by simulating each anomaly type against a test device and inspecting the logs.&nbsp;</li><li>Alerts include at minimum the source device IP, the anomaly type, and a timestamp, confirmed by inspecting a sample alert entry from each detection script.&nbsp;</li></ol></td><td><p>Important for&nbsp;identifying<br>compromised devices but it depends on FR-06 being in place.&nbsp;</p></td></tr><tr><td><p>FR-10</p></td><td><p>REST API for Policy Management</p></td><td><p><strong>Could</strong></p><p><strong>Have</strong></p></td><td><ol><li>A POST request to the isolation endpoint correctly isolates the target device, confirmed by issuing the request and verifying the device loses connectivity and the flow rules are installed.</li><li>A POST request to the release endpoint restores normal policy for the target device, confirmed by issuing the request and verifying the device regains connectivity appropriate to its profile.</li><li>The API returns a meaningful error response when an invalid MAC address or unknown device is supplied, confirmed by submitting malformed requests and inspecting the response body.</li></ol></td><td><p>Strengthens the security posture by reducing response time and removes the need for a person to&nbsp;monitor&nbsp;the system 24/7,&nbsp;but&nbsp;isolation can be triggered manually via the REST API in FR-06.&nbsp;Automated isolation adds complexity and creates a risk of false positives.&nbsp;&nbsp;</p></td></tr><tr><td colspan="5"><p><strong>Non-Functional Requirements</strong></p></td></tr><tr><td><p>NFR-01</p></td><td><p>Wireless Security</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>Gateway supports WPA3 and WPA2-PSK.</li><li>Confirmed by a test device completing the connection handshake without error using WPA2-PSK credentials and WPA3 credentials.</li><li>IoT devices connected with different security modes should never be allowed to communicate.</li></ol></td><td><p>WPA3 offers greater security the WPA2-PSK, however, many IoT devices only support WPA2-PSK. Enabling support for both ensures security and usability, but it is not necessary for the system to function.</p></td></tr><tr><td><p>NFR-02</p></td><td><p>External Network Invisibility</p></td><td><p><strong>Must</strong></p><p><strong>have</strong></p></td><td><ol><li>A port scan of the IoT subnet conducted from an external host should returns no reachable addresses. Confirmed by running an NMAP scan of the IoT subnet from an external host.</li><li>An unsolicited inbound connection attempt from an external host to an IoT device IP address fails should fail to reach the device, confirmed by running a connection attempt from outside the network and observing no response.</li></ol></td><td><p>If IoT devices were directly addressable from outside the network, it would greatly increase the attack surface. The inbound attack surface would need to be monitored and controlled separately.</p></td></tr><tr><td><p>NFR-03</p></td><td><p>Granular control of network micro segmentation</p></td><td><p><strong>Could</strong></p><p><strong>have</strong></p></td><td><ol><li>System administrator can specify that IoT device A is allowed to communicate with IoT device B on the network if necessary.</li><li>Confirmed by sending an ICMP ping between two IoT devices on the network and seeing 100% packet loss, then using the REST API to modify access policies and testing the ICMP ping again and seeing 0% packet loss.</li></ol></td><td><p>Although IoT devices should generally not need to communicate with each other over Wi-Fi, there may be some cases where it is needed. Giving the network administrator the option provides flexibility; however, it is not required for the system to function and preventing lateral movement should be prioritized.</p></td></tr><tr><td><p>NFR-04</p></td><td><p>Performance</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>The gateway sustains normal operation with at least 20 concurrently connected IoT devices without packet loss attributable to policy enforcement.</li><li>Round-trip latency for a DNS query from an IoT device does not exceed 50ms under normal load, measured with dig.</li><li>CPU utilisation across all containers does not exceed 80% sustained under normal IoT traffic load, measured via docker stats.</li></ol></td><td><p>The system must be usable in practice. Excessive latency or dropped packets would impact legitimate device operation and undermine confidence in the gateway.</p></td></tr><tr><td><p>NFR-05</p></td><td><p>Reliability and Resilience</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>All components and network configurations are configured with restart. Verified by shutting down each component and testing that it is functional, then checking that an appropriate warning was logged.</li><li>If a component goes offline, a warning should be logged. All other components should remain running and resume normal operation once the offline component returns. Verified by disabling a components dependency, then re enabling them and verifying normal operation is resumed.</li><li>If a core networking component goes offline the default fall back behaviour should be to deny all traffic. Verified by disabling component responsible for layer 2 switching and attempting an ICMP ping from a connected host to an external IP and a device on the local network.</li></ol></td><td><p>Manual intervention after a system failure would create a window of exposure. Automatic recovery is essential for unattended operation.</p></td></tr><tr><td><p>NFR-06</p></td><td><p>Alerting for unencrypted traffic</p></td><td><p><strong>Could</strong></p><p><strong>have</strong></p></td><td><ol><li>The system must detect an alert when an IoT device initiates an unencrypted outbound connection. Confirmed by sending HTTP requests from an IoT device to an external host, then checking the logs for an alert.</li></ol></td><td><p>Unencrypted traffic may be sniffed over the air or downstream. Many IoT devices use unencrypted protocols such as HTTP, so it cannot be blocked entirely. Alerting for it creates awareness and allows the system administrator to verify no confidential information is leaded.</p></td></tr><tr><td><p>NFR-07</p></td><td><p>Maintainability</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>A new allowlist can be added and applied to the running system via the REST API without restarting any components, verified by adding a profile and confirming enforcement takes effect immediately.</li><li>Device allow lists and known bad domain lists can be edited whilst the system is running without restarting any components. Verified by sending ICMP ping to an IP and confirming 100% loss, then adding the IP to the device allowlist whilst the system is running and running the ICMP ping again and confirming 0% loss.</li><li>Detection thresholds for automated detection scrips can be updated without modifying the code. Verified by generating fake malicious traffic and checking detection in the logs, then updating threshold configuration file and observing the change in detection thresholds in the logs.</li></ol></td><td><p>System configuration files such as device allowlists, IOCs such as known bad domains will need updating over time. A system that requires restarts or code changes for routine updates is not practical.</p></td></tr><tr><td><p>NFR-08</p></td><td><p>Log maintenance</p></td><td><p><strong>Should</strong></p><p><strong>have</strong></p></td><td><ol><li>Logs&nbsp;are automatically archived daily and archive files&nbsp;are timestamped, confirmed by&nbsp;observing&nbsp;the log directory after one rotation cycle.&nbsp;</li><li>Archived logs are automatically purged after 90 days, verified by confirming the retention job is scheduled and that no archived log files older than 90 days are present on the filesystem.</li></ol></td><td><p>Monitoring all network traffic on a network with up to 20 IoT devices will create a lot of log data. Without proper log maintenance a very large amount of storage will be required; this would unnecessarily increase the hardware requirements of the system.</p></td></tr><tr><td><p>NFR-09</p></td><td><p>Portability</p></td><td><p><strong>Could have</strong></p></td><td><ol><li>Components that are not reliant on interacting with the host systems hardware should be containerised to allow for portability and platform independence.</li><li>No component depends on an external cloud service or remote API to function, verified by confirming the system operates correctly with the WAN interface disconnected from the upstream router.</li></ol></td><td><p>Containerisation of components makes the system reproducible and easier to evaluate, and version control will be easier when making future improvements; however, the system still fulfils its security purpose without it.</p><p>Not relying on external dependencies means the system cannot be brought offline by an outage with one of the external dependencies. This reduces risk but is not required for the system to function.</p></td></tr><tr><td><p>NFR - 10</p></td><td><p>Observability Stack</p></td><td><p><strong>Won’t</strong></p><p><strong>have</strong></p></td><td><ol><li>Implementation of an open-source observation stack such as Elastic stack for processing, visualization and querying of log data and alters.</li></ol></td><td><p>Whilst this would be a requirement in a production environment, this system is being developed as a proof of concept. Integration of an observability stack would be a significant scope expansion that would not increase the systems security capabilities.</p></td></tr><tr><td><p>NFR-11</p></td><td><p>Wi-Fi Captive Portal</p></td><td><p><strong>Won’t have</strong></p></td><td><p>1. Implementation of a captive portal for device onboarding when a new device joins the network.</p></td><td><p>This would be helpful in a production environment to allow a user to specify information about their device when it first joins, however it is not necessary for this proof of concept as I can manually update a devices profile, and I will know the details of every test device.</p></td></tr></tbody></table></div>